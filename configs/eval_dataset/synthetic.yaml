# Synthetic test dataset — fresh graphs generated with a separate test seed.

dataset: synthetic

# Override the data generation seed with the eval-specific test seed.
# Resolves from cfg.eval_mode.test_seed (the eval_mode config group key).
data:
  seed: ${eval_mode.test_seed}
