# pixi run train --multirun data.outlier_rate=10,30,50,70,90 data.outlier_structure=random,clustered
# Add +sweep=outlier_grid hydra/launcher=joblib hydra.launcher.n_jobs=N for parallelism

hydra:
  sweeper:
    params:
      data.outlier_rate: 10,30,50,70,90
      data.outlier_structure: random,clustered
